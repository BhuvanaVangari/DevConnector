spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/dnb3
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
  jackson:
    serialization:
      fail-on-empty-beans: false
server:
  port: 9002
  error:
    include-message: always